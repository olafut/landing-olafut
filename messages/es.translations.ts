import { AboutUs, Blog, Home, Layout, NotFound } from './es';
import type { Translations } from './types';

const translations: Translations = {
  Layout,
  Home,
  AboutUs,
  Blog,
  NotFound,
};

export default translations;
